# Advisory scoring

`data/shared/scoring.yaml` defines the base score, bounds, preferred length, and each additive signal. Reports include every applied signal and value. Current signals cover resolved criteria, category, preferred length, similarity, blocklist warning, missing provenance, and unresolved criteria.

**Scores do not approve or reject words.** They are deterministic navigation aids for human review and never mutate candidate state.

