# Similarity analysis

LexiForge deterministically compares normalized words within one language using Damerau–Levenshtein distance, including insertion/deletion and adjacent transposition. Distance-one and affix-like pairs are shown once in lexical pair order.

Similarity findings are warnings for human review, not proof that either word is unsuitable. They do not automatically exclude an otherwise eligible candidate.

